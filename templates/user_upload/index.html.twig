{% extends 'userProfil.html.twig' %}

{% block title %}Vos Upload{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Vos Uploads ({{app.user.uploads|length}})</h1>
</div>
    <div href="#upload" class="grix xs6">
        {% for contact in app.user.uploads %}
            <div class="pos-xs1"> File Name : </div>
            <div class="pos-xs2">{{upload.files}}</div>
            <div class="pos-xs3">  |  </div>
            <div class="pos-xs1"> Description  : </div>
            <div class="pos-xs2">{{upload.description}}</div>
        {% endfor %}
</div>
{% endblock %}
