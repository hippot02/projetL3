{% extends 'userProfil.html.twig' %}

{% block title %}Vos Coins
{% endblock %}

{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>

	<div class="example-wrapper">
		<h1>
			Vos Coins ! ✅</h1>
		<h2>
			{% for role in app.user.roles %}
				{% if role == "ROLE_USER" or role =="ROLE_PRIME" and role != "ROLE_ADMIN" %}
					{{app.user.coins}}
				{% elseif role == "ROLE_ADMIN"%}
					<a href="{{path('app_user_coin_modif')}}" class="btn blue">
						Modifier les Coins
					</a>
				{% endif %}
			{% endfor %}
		</h2>


	</div>
{% endblock %}
